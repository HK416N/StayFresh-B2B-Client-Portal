info:
  name: server health check
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
